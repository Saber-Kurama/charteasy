```javascript

const sZ = e => {
  const { language: t, visualization: i, onReady: n, onUpdate: r } = e,
  o = function(e) { /* theme/canvas config extraction */ }(i),
  [s, l] = useState(o.theme),  // theme config
  [c, d] = useState(o.canvas), // canvas config
  h = useRef(null)
  
  return (
    <div className="visualization-editor-panel">
      <div className="visualization-editor-panel-header">
        <div className="visualization-editor-panel-header-tools">
          <$W language={t} canvasConfig={c} onChange={...} />  {/* Canvas config */}
          <GW language={t} themeConfig={s} onChange={...} />   {/* Theme config */}
        </div>
        <RW title={i.name} language={t} onUpdate={...} />      {/* Title */}
        <div className="visualization-editor-panel-header-tools">
          <aZ visualization={i} language={t} />                 {/* Export */}
          <oZ language={t} editor={...} updateEditor={...} />   {/* Preview */}
        </div>
      </div>
      <div className="visualization-editor-panel-content">
        <PW visualizationId={i.id} ref={h} ... />               {/* Editor content */}
      </div>
    </div>
  )
}

```




``` javascript

const PW = forwardRef((e, t) => {
  const {
    visualizationId: i,
    language: n,
    initialEditorData: r,
    dataVersion: o,
    onReady: s,
    onUpdate: l
  } = e
  
  const c = useRef(null), d = useRef(null)
  
  // Imperative handle for parent component
  useImperativeHandle(t, () => ({
    getEditor: () => d.current?.editor,
    updateEditor: () => { d.current?.updateEditor() },
    setEditorTheme: e => {
      // Theme switching logic with custom colors
      // ...
      t.updateCustomTheme(a, !1, !0), t.editorData.saveData()
    }
  }))
  
  // Environment config
  const p = { ...PV,
    getLanguage: () => n,
    writeData: async () => { ... },
    readData: async () => { ... },
    getContainerRect: async () => ({ width, height }),
    getShareLinkKey: async () => `online_app_${i}`
  }
  
  // Handlers
  const h = { onReady: e => { console.log("onReady", e), s() } }
  
  return r ? (
    <div className="app-editor-container" ref={c}>
      <wV ref={d} env={p} handlers={h} featureMap={BV} />
    </div>
  ) : null
})

```




EditApp


```javascript
wV = forwardRef((e, t) => {
  const { handlers: i, guideVisible: n = !1 } = e,
  r = yC(e),  // ready state
  { setLanguage: s } = Ok(...),
  { editor: l, updateEditor: c } = jS(...),
  { setAIAssistantType: d } = Hw(...);
  
  bC(t, l, c);  // bind ref to editor state
  
  useEffect(() => {
    r && (i?.onInit?.(), G_.performanceMarkEnd(...), window.focus(), ...)
  }, [r]);
  
  useEffect(() => {
    if (l && u_(s_.AI_ASSISTANT_INITIAL_DISPLAY)) {
      const e = l.getChartElements();
      e.length > 0 && l.selectElement(e[0].id), d("editor")
    }
  }, [l]);
  
  return r ? (
    <div className="edit-app">
      <QL handlers={i} />      {/* Top toolbar */}
      <AV />                   {/* AI Assistant */}
      <wL handlers={i} />      {/* Left toolbar */}
      {l ? (
        <>
          <EL guideVisible={n} />  {/* Guide overlay */}
          <mV />                    {/* Main canvas */}
          <oG />                    {/* Overlays */}
          {u_(s_.DATA_EDIT) && <aL />}  {/* Data edit panel */}
          <sG handlers={i} />       {/* Selection panel */}
          <_G />                    {/* Additional overlays */}
        </>
      ) : null}
      <lG />                   {/* Layer panel */}
    </div>
  ) : null
});

```

